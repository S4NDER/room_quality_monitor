<%= form_with(model: humidity, local: true) do |form| %>
  <% if humidity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(humidity.errors.count, "error") %> prohibited this humidity from being saved:</h2>

      <ul>
      <% humidity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value, id: :humidity_value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
