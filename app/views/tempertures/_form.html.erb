<%= form_with(model: temperture, local: true) do |form| %>
  <% if temperture.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(temperture.errors.count, "error") %> prohibited this temperture from being saved:</h2>

      <ul>
      <% temperture.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value, id: :temperture_value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
