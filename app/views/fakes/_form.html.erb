<%= form_with(model: fake, local: true) do |form| %>
  <% if fake.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fake.errors.count, "error") %> prohibited this fake from being saved:</h2>

      <ul>
      <% fake.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
