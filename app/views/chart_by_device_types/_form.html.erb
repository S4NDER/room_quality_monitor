<%= form_with(model: chart_by_device_type, local: true) do |form| %>
  <% if chart_by_device_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(chart_by_device_type.errors.count, "error") %> prohibited this chart_by_device_type from being saved:</h2>

      <ul>
      <% chart_by_device_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
