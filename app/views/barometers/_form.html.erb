<%= form_with(model: barometer, local: true) do |form| %>
  <% if barometer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(barometer.errors.count, "error") %> prohibited this barometer from being saved:</h2>

      <ul>
      <% barometer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value, id: :barometer_value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
